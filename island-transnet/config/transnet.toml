# Main Transnet configuration

[server]
host = "${BACKEND_HOST}"
port = "${BACKEND_PORT}"
workers = "${WORKERS}"

[logging]
level = "${RUST_LOG}"
format = "json"
file = "logs/transnet.log"

[ipc]
socket_path = "/tmp/transnet_ipc.sock"
enable = true
