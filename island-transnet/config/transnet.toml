# Runtime configuration consumed by transnet-core.

[server]
host = "127.0.0.1"
port = 35792
# Reserved for a future explicit Tokio runtime; currently reported in startup logs only.
workers = 4

[logging]
level = "info"
format = "json"
# Reserved for file logging; the current server writes to standard output.
file = "logs/transnet.log"
